This directory is intentionally shipped without model binaries.

Run setup_local_models.py during the approved installation phase. It downloads:
- face_landmarker.task from the official Google MediaPipe model storage
- hand_landmarker.task from the official Google MediaPipe model storage

The review command loads these files locally and does not download them.
